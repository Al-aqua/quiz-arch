import Root, {
    type ButtonProps,
    type ButtonSize,
    type ButtonVariant,
    buttonVariants,
} from "./button.svelte";

import { default as Button } from "./Button.svelte";

export {
    Root,
    type ButtonProps as Props,
    //
    Button,
    buttonVariants,
    type ButtonProps,
    type ButtonSize,
    type ButtonVariant,
};
